const blog = [
  {
    title: "How to Convert HTML Form Field Values to a JSON Object",
    subTitle: "How to Convert HTML Form Field Values to a JSON Object",
  },
  {
    content: [
      { heading: "Are you more automatic or manual." },
      {
        paragraph:
          `Some servers and clients cannot correctly process some HTTP methods that the API makes use of. 
          For example, all deletion requests on resources use the DELETE method, but some clients do not 
          provide the ability to send this method.`,
      },
      {
        paragraph:
          `Some servers and clients cannot correctly process some HTTP methods that the API makes use of. 
          For example, all deletion requests on resources use the DELETE method, but some clients do not 
          provide the ability to send this method.`,
      },
      {
        image: "http://www"
      },
      { heading: "Are you more automatic or manual." },
      {
        paragraph:
          `Some servers and clients cannot correctly process some HTTP methods that the API makes use of. 
          For example, all deletion requests on resources use the DELETE method, but some clients do not 
          provide the ability to send this method.`,
      },
      {
        paragraph:
          `Some servers and clients cannot correctly process some HTTP methods that the API makes use of. 
          For example, all deletion requests on resources use the DELETE method, but some clients do not 
          provide the ability to send this method.`,
      },
      {
        list: [
            
        ]
      }
    ],
  },
];
